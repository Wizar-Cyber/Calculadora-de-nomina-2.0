[build]
  command = "npm install --prefix frontend && npm run build --prefix frontend"
  publish = "frontend/.next"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[build.environment]
  NODE_VERSION = "20"
