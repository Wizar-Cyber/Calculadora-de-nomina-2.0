[build]
  command = "npm install --prefix frontend && npm run export --prefix frontend"
  publish = "frontend/out"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
